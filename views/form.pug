extends layout.pug 

block content
  form(action="/new" method="POST" class="new-message")
    textarea(name="msg" placeholder="what do want to write?" cols="30" rows="10" required)
    input(name="author" placeholder="your name, uwu" type="text" minlength="3" required)
    button(type="submit") Send
  p: a(href='/') Read letters
  